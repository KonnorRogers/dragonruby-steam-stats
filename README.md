# A really bad attempt at using File Dialogs in DragonRuby.

(This does not work currently due to native-file-dialog-extended expecting to be on the main UI thread, and because DR does not expose the SDL window object)
